{
  plugins.auto-session = {
    enable = true;
    settings = {
      auto_restore = true;
      auto_save = true;
      use_git_branch = true;
      auto_restore_last_session = false;
    };
  };
}
