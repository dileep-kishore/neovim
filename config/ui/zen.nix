{
  plugins.twilight = {
    enable = true;
  };
  plugins.zen-mode = {
    enable = true;
    settings = {
      plugins = {
        twilight = {
          enabled = false;
        };
      };
      window = {
        backdrop = 1;
      };
    };
  };
}
